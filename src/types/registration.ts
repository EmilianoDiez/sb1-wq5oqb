export interface AffiliateFormData {
  name: string;
  email: string;
  phone: string;
  dni: string;
}

export interface CompanionData {
  name: string;
  age: string;
  dni: string;
  phone?: string;
}